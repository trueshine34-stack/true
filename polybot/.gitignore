node_modules/
dist/
.DS_Store
*.local

# Android build output
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
android/capacitor-cordova-android-plugins/
*.keystore
*.jks
# …except the app's own signing key. A build from a different machine must
# produce the same signature, or Android treats it as a different app: the
# install then wipes the data, and the sealed wallet key with it.
!android/keystore/polybot.jks

# Locally staged APKs

# TypeScript incremental build state
tsconfig.tsbuildinfo
